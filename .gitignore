node_modules
.next
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts.bak

# Server state (local fs-backed store)
data/

# Bootstrap push state — contains private key + repo key plaintext
.siphr-push.json
